/// <reference types="react" />
import React from 'react';
import { FontCommon } from "./common-03e05004";
import { CaptionVariant, TextSize, TitleVariant } from "./types-d241be51";
type LinkSizes = Exclude<TextSize | TitleVariant | CaptionVariant, 'inherit'>;
type ExtendWithProps = Pick<FontCommon<null>, 'dataTestId' | 'highlightingColor' | 'lineThrough' | 'fontIndex' | 'children' | 'color'>;
interface LinkProps extends ExtendWithProps {
    /** Путь ссылки */
    href?: string;
    /** Размер ссылки */
    size?: LinkSizes;
    /** Флаг, жирное начертание */
    bold?: boolean;
    /** Флаг, курсивное начертание */
    italic?: boolean;
    /** Атрибут target */
    target?: string;
    /** Атрибут rel */
    rel?: string;
    /** Колбэк нажатие на ссылку */
    onClick?: React.MouseEventHandler<HTMLAnchorElement>;
}
export { LinkSizes, LinkProps };
