import { ReactElement } from 'react';
/** Рекурсивно пройтись по дочерним компонентам, выполнив для них переданный метод */
declare const recursiveMap: (children: ReactElement | ReactElement[], fn: (child: ReactElement) => ReactElement) => ReactElement[];
export { recursiveMap };
