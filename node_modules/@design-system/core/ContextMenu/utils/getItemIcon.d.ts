import { ElementType } from 'react';
interface ItemIconProps {
    /** Это подменю? */
    isSubMenu: boolean;
    /** На этом уровне есть пункты меню с иконками? */
    withIcons?: boolean;
    Icon?: ElementType;
}
/** Получить иконку / заполнитель иконки для пункта меню */
declare function getItemIcon({ isSubMenu, withIcons, Icon }: ItemIconProps): ElementType | undefined;
export { getItemIcon };
