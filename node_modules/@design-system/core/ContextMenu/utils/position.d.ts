import { MousePosition, MenuPosition } from "../../types-9f6a6544";
/** Определить позицию меню */
declare const calculateMenuPosition: (menu: HTMLUListElement, mousePosition: MousePosition, srcMenuHeight: number) => MenuPosition;
/** Определить позицию подменю */
declare const calculateSubmenuPosition: (parentNode: HTMLLIElement, elementsCount: number) => MenuPosition;
export { calculateMenuPosition, calculateSubmenuPosition };
