import { SizeVariant } from "./common-03e05004";
type RadioButtonSize = Exclude<SizeVariant, 'large'>;
interface RadioButtonProps {
    /** Значение (уникальное для каждой RadioButton в группе) */
    value: string;
    /** Ярлык радиокнопки */
    label: string;
    /** Имя-идентификатор группы, к которой принадлежит радиокнопка */
    name: string;
    /** Размер радиокнопки */
    size?: RadioButtonSize;
    /** Состояние кнопки (checked / non-checked ) */
    checked?: boolean;
    /** Состояние кнопки ( enabled / disabled ) */
    disabled?: boolean;
    /** Задает механизм управления состоянием checked */
    uncontrolled?: boolean;
    /** Ограничение контейнера текста радиокнопки, в px (если текст длиннее, то будет обрезан с многоточием в конце)  */
    textLimitWithEllipsis?: number;
    /** Ограничение строки для переноса, в px (если текст длиннее, будет перенос строки),
     * Если указано TextLimitWithEllipsis, то игнорируется */
    labelMaxWidth?: number;
}
type RadioButtonGroupItem = Pick<RadioButtonProps, 'value' | 'label' | 'disabled'>;
interface RadioButtonGroupProps {
    /** Имя-идентификатор группы радиокнопок */
    name: string;
    /** Ярлык группы радиокнопок */
    label?: string;
    /** Массив радиокнопок */
    items: RadioButtonGroupItem[];
    /** Коллбэк-функция, вызываемая при переключении (в controlled-режиме: при попытке переключения) активной радиокнопки
     * @param {string} value - значение value выбранной кнопки
     */
    onChange: (value: string, fieldName?: string) => void;
    /** Размер группы радиокнопок */
    size?: RadioButtonSize;
    /** Отображение ярлыка слева (по умолчанию: сверху) */
    labelAside?: boolean;
    /** Активная радиокнопка (в controlled-режиме: текущая, в uncontrolled-режиме: по умолчанию) */
    activeValue?: string;
    /** Флаг disabled */
    disabled?: boolean;
    /** Флаг uncontrolled */
    uncontrolled?: boolean;
    /** Ширина label */
    labelWidth?: number;
    /** ID для тестирования */
    dataTestId?: string;
    /** Имя поля, передается в onChange дополнительным аргументом */
    fieldName?: string;
    /** Ограничение контейнера текста радиокнопки, в px (если текст длиннее, то будет обрезан с многоточием в конце)  */
    textLimitWithEllipsis?: number;
    /** Ограничение строки для переноса, в px (если текст длиннее, будет перенос строки),
     * Если указано TextLimitWithEllipsis, то игнорируется */
    labelMaxWidth?: number;
}
export { RadioButtonSize, RadioButtonProps, RadioButtonGroupItem, RadioButtonGroupProps };
