import { BaseInputSizeCore } from "./types-3e12d472";
import { ShareableInputProps } from "./input-3e12d472";
type TextareaSize = Exclude<BaseInputSizeCore, 'large'>;
type ExcludeShareableInputProps = 'inputType' | 'icons' | 'hint' | 'inputRef';
interface TextareaProps extends Omit<ShareableInputProps, ExcludeShareableInputProps> {
    /** Количество отображаемых строк используется для задания высоты */
    rows?: number;
    /** подстраивать высоту под введенный текст   */
    stretch?: boolean;
    /** Размеры отступов, шрифта и других свойств у textarea label */
    size?: TextareaSize;
    /** Ярлык */
    label?: string;
    /** Ширина label */
    labelWidth?: number;
    /** Счетчик количества введенных символов */
    counter?: boolean;
    /** Текст подсказки / сообщения об ошибке */
    hintMessage?: string;
}
interface CountProps {
    /** Отображать ли счетчик длинные текста */
    show: boolean;
    /** Переводит состояния счетчика теста в неактивный, заменяет стиль (тест становиться тусклым) */
    disabled: boolean;
    /** Текущая длинна теста */
    textLength: number;
    /** Показывает максимальное кол-во введенного текста */
    maxLength: number | undefined;
    /** Подстраивает размеры блока */
    size?: TextareaSize;
}
export { TextareaSize, TextareaProps, CountProps };
