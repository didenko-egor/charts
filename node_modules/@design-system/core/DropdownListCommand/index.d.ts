/// <reference types="react" />
import React from "react";
import { ElementType } from "react";
interface DropdownListCommandProps {
    /** Текст пункта меню */
    label?: string;
    /** Иконка */
    Icon?: ElementType;
    /** Текстовая подсказка или иконка */
    hint?: ElementType | string;
    /** Есть ли вложенные пункты меню */
    withSubmenu?: boolean;
    /** Идентификатор вложенного меню */
    menuId?: string;
    /** Имеются ли иконки */
    withIcons?: boolean;
    /** Размер меню */
    size?: "default" | "small";
    /** Флаг disabled */
    disabled?: boolean;
    /** Флаг включает управления ховером извне для данного элемента */
    controlledHover?: boolean;
    /** Флаг говорит о том, что ховер будет работать для элемента по умолчанию (появляться только при наведении) */
    uncontrolledHover?: boolean;
    /** Задает произвольную ширину элемента */
    width?: number;
    /** testId атрибут для тестирования */
    dataTestId?: string;
    /** Функция, вызываемая по клику на элемент */
    onClick?: VoidFunction;
    /** Функция, вызываемая перед кликом на элемент */
    onMouseDown?: VoidFunction;
    /** Функция, вызываемая при наведении курсора на элемент */
    handleMouseEnter?: VoidFunction;
    /** Функция, вызываемая при перемещении курсора с элемента */
    handleMouseLeave?: VoidFunction;
}
declare const _default: React.MemoExoticComponent<React.ForwardRefExoticComponent<DropdownListCommandProps & React.RefAttributes<HTMLLIElement>>>;
export { _default as default, DropdownListCommandProps };
