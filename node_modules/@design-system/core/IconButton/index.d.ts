/// <reference types="react" />
import React from 'react';
import { ButtonCoreSize, ButtonCoreType, ShareableButtonProps } from "../button-42e030aa";
declare const DEFAULT_PROPS: {
    type: string;
    size: string;
};
interface IconButtonProps extends Omit<ShareableButtonProps, 'iconLeft' | 'iconRight' | 'isIcon'> {
    /** Размер кнопки */
    size?: ButtonCoreSize;
    /** Тип кнопки */
    type?: ButtonCoreType;
    /** Контент в виде иконки */
    icon: React.ReactElement;
    /** Пропс для растягивания кнопки на ширину родительского контейнера */
    stretch?: boolean;
}
declare const IconButton: React.FC<IconButtonProps>;
export { DEFAULT_PROPS, IconButtonProps, IconButton };
