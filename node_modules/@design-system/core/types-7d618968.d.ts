import { ReactNode } from 'react';
import { FontColorAliases } from "./common-03e05004";
import { TextSize } from "./types-03e05004";
type BreadcrumbsSize = Exclude<TextSize, 'extra_large' | 'large' | 'inherit'>;
type BreadcrumbColor = Exclude<FontColorAliases, 'primary' | 'secondary' | 'tertiary' | 'disabled' | 'text_tertiary' | 'text_disabled' | 'text_primary_inverse' | 'link_primary' | 'link_secondary' | 'link_tass'>;
interface ICrumb {
    /** Заголовок хлебной крошки */
    title: string;
    /** Путь для роутинга */
    fullPath?: string;
    /** Функция клика на хлебную крошку */
    onClick?(): void;
    /** Ялвяется ли хлебная крошка троеточием? */
    isCollapsed?: boolean;
}
interface BreadcrumbsProps {
    /** Массив хлебных крошек */
    breadcrumbs: ICrumb[];
    /** Флаг отвечает за отоборажения только путя, без возможности перехода по хлебным крошкам */
    onlyPath: boolean;
    /** Цвет хлебных крошек */
    color?: BreadcrumbColor;
    /** Размер */
    size?: BreadcrumbsSize;
    /** Отвечает за подсветку активного (последнего) элемента */
    highlightActive?: boolean;
    /** Функция для рендера компонента Link */
    renderCrumbLink(href: string, children: ReactNode): ReactNode;
    dataTestId?: string;
}
interface BreadcrumbItemProps {
    /** Является ли хлебная крошка активной (последней)? */
    active?: boolean;
    /** Ссылка для роутера */
    href?: string;
    /** Прокидываемый компонент */
    children?: ReactNode;
    /** Флаг, который отвечает за text-overflow ellipsis */
    overflow?: boolean;
    /** Цвет текста */
    color?: BreadcrumbColor;
    /** Размер */
    size?: BreadcrumbsSize;
    /** Флаг, который отвечает за подчеркивание */
    withUnderline?: boolean;
    /** Является ли хлебная крошка Collapsed? */
    isCollapsed?: boolean;
    /** Отвечает за подсветку активного (последнего) элемента */
    highlightActive?: boolean;
    /** Функция клика по хлебной крошки */
    onClick?(): void;
    /** Функция для рендера компонента Link */
    renderCrumbLink(href: string, children: ReactNode): ReactNode;
}
export { BreadcrumbsSize, BreadcrumbColor, ICrumb, BreadcrumbsProps, BreadcrumbItemProps };
