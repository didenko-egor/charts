/// <reference types="react" />
import { SelectedDate } from "../types";
declare const START_YEAR_OFFSET = 4;
interface YearsPageProps {
    /** Год, который будет в середине страницы */
    activeYear: number;
    /** Выбранная дата */
    selectedDate?: SelectedDate;
    /** Минимальная дата */
    minDate?: Date;
    /** Максимальная дата */
    maxDate?: Date;
    /** Показать страницу месяцев для года */
    onShowMonths?: (year: number) => void;
    /** Показать предыдущую страницу лет */
    onPrev?: VoidFunction;
    /** Показать следующую страницу лет */
    onNext?: VoidFunction;
}
/** Страница годов */
declare function YearsPage({ activeYear, selectedDate, minDate, maxDate, onShowMonths, onPrev, onNext }: YearsPageProps): JSX.Element;
export { START_YEAR_OFFSET, YearsPageProps, YearsPage };
