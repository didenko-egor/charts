import { SelectedDate } from "../types";
/** Проверить, вписывается ли дата в заданный диапазон */
declare function dayDisabled(day: Date, minDate?: Date, maxDate?: Date): boolean;
/** Определить дата выбрана или нет */
declare function daySelected(day: Date, selectedDate?: SelectedDate): boolean;
/** Определить, находится ди дата в диапазоне */
declare function dayInRage(day: Date, selectedDate?: SelectedDate): boolean;
export { dayDisabled, daySelected, dayInRage };
