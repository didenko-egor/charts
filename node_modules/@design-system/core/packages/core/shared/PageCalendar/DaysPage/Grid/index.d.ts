/// <reference types="react" />
import { DayValue } from "../../types";
interface DaysGridProps {
    /** Даты для отображения. undefined - для пустых ячеек */
    items: Array<DayValue | undefined>;
    /** Подписи дней недели */
    weekDays?: string[];
    /** Скрыть выходные? */
    hideWeekends?: boolean;
    /** Изменена выбранная дата */
    onChange?: (value: DayValue, index?: number) => void;
}
/** Сетка дней в месяце */
declare function DaysGrid({ items, weekDays, hideWeekends, onChange }: DaysGridProps): JSX.Element;
export { DaysGridProps, DaysGrid };
