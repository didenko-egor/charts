/// <reference types="react" />
import { DayValue, LocaleOptions, SelectedDate } from "../types";
interface DaysPageProps {
    /** Выбранная дата/диапазон */
    selectedDate?: SelectedDate;
    /** Месяц, год на котором должна отображаться страница */
    activeDate: Date;
    /** Минимальная дата */
    minDate?: Date;
    /** Максимальная дата */
    maxDate?: Date;
    /** Скрыть отображение выходных? */
    hideWeekends?: boolean;
    /** Отмеченные дни */
    marks?: Date[];
    /** Параметры локализации */
    localeOptions?: LocaleOptions;
    /** Выбранная дата изменилась */
    onChange?: (value: DayValue) => void;
    /** Показать страницу месяцев */
    onShowMonths?: (year: number) => void;
    /** Показать страницу годов */
    onShowYears?: (year: number) => void;
    /** Показать предыдущий месяц */
    onPrev?: VoidFunction;
    /** Показать следующий месяц */
    onNext?: VoidFunction;
}
/** Страница дней */
declare function DaysPage({ activeDate, selectedDate, minDate, maxDate, hideWeekends, marks, localeOptions, onChange, onShowYears, onShowMonths, onPrev, onNext }: DaysPageProps): JSX.Element;
export { DaysPageProps, DaysPage };
