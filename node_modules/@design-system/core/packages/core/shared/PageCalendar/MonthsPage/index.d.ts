/// <reference types="react" />
import { SelectedDate } from "../types";
interface MonthsPageProps {
    /** Год */
    activeYear: number;
    /** Названия месяцев */
    monthsNames?: string[];
    /** Выбранная дата */
    selectedDate?: SelectedDate;
    /** Минимальная дата */
    minDate?: Date;
    /** Максимальная дата */
    maxDate?: Date;
    /** Показать страницу годов */
    onShowYears?: (year: number) => void;
    /** Показать страницу дней */
    onShowDays?: (month: number, year: number) => void;
    /** Перейти к следующему году */
    onPrev?: VoidFunction;
    /** Перейти к предыдущему году */
    onNext?: VoidFunction;
}
declare function MonthsPage({ activeYear, monthsNames, selectedDate, minDate, maxDate, onShowYears, onShowDays, onPrev, onNext }: MonthsPageProps): JSX.Element;
export { MonthsPageProps, MonthsPage };
