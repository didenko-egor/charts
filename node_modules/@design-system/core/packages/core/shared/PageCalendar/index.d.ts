/// <reference types="react" />
import { CalendarInitialState, LocaleOptions, SelectedDate } from "./types";
interface PageCalendarProps {
    /** Выбранная дата */
    value?: SelectedDate;
    /** На какой дате и странице открыть календарь */
    openAt?: CalendarInitialState;
    /** Минимальная дата */
    minDate?: Date;
    /** Максимальная дата */
    maxDate?: Date;
    /** Не отображать выходные? */
    hideWeekends?: boolean;
    /** Отмеченные дни */
    marks?: Date[];
    /** Включить режим выбора диапазона */
    rangeMode?: boolean;
    /** Параметры локализации */
    localeOptions?: LocaleOptions;
    /** Выбранная дата изменилась */
    onChange?: (value: SelectedDate) => void;
    /** Нажатие на любой кнопке календаря. Используется, чтобы вернуть фокус куда нужно */
    onClick?: VoidFunction;
}
/** Страничный календарь */
declare function PageCalendar({ minDate, maxDate, hideWeekends, marks, value, openAt, rangeMode, localeOptions, onChange, onClick }: PageCalendarProps): JSX.Element;
export { PageCalendarProps, PageCalendar };
