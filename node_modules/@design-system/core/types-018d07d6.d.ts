/// <reference types="react" />
type ControlButtons = {
    /** Контент в виде иконки */
    icon: JSX.Element;
    /** Колбек по нажатию на кнопку */
    onClick: VoidFunction;
    /** Флаг для блокировки кнопки */
    disabled?: boolean;
};
type ControlStyle = 'default' | 'float';
export { ControlButtons, ControlStyle };
