/// <reference types="react" />
import React from 'react';
import { ReactNode } from "react";
import { ShareableButtonProps, ButtonExternalType, ButtonExternalSize, ButtonCoreType, ButtonCoreSize, ButtonBaseStyles } from "../../button-42e030aa";
declare const DEFAULT_PROPS: {
    rel: string;
    role: string;
    ariaLabel: string;
    target: string;
    disabled: boolean;
};
interface BaseButtonProps extends ShareableButtonProps {
    /** Стили  кнопки */
    styles: ButtonBaseStyles;
    /** Тип кнопки */
    type?: ButtonExternalType | ButtonCoreType;
    /** Размер кнопки */
    size?: ButtonExternalSize | ButtonCoreSize;
    /** Контент кнопки */
    content: string | React.ReactElement;
    /** Пропс для растягивания кнопки на ширину родительского контейнера */
    stretch?: boolean;
    /** Флаг, акцентное отображение кнопки для Secondary и Tertairy варианта отображения(только External) */
    accent?: boolean;
    /** Флаг траурного режима */
    isMourning?: boolean;
    /** Функция принимающая поле size и возвращаюшая loader */
    renderLoader?: (arg: ButtonExternalSize | ButtonCoreSize | undefined) => ReactNode;
}
declare const BaseButton: ({ onClick, type, content, href, rel, role, target, ariaLabel, disabled, dataTestId, styles, size, iconLeft, iconRight, isIcon, stretch, accent, isLoading, isMourning, renderLoader }: BaseButtonProps) => JSX.Element;
export { DEFAULT_PROPS, BaseButtonProps, BaseButton };
