/// <reference types="react" />
import { ShareableInputProps } from "./input-3e12d472";
import { ComponentStyles } from "./common-70c85551";
type BaseInputSize = BaseInputSizeCore | BaseInputSizeExternal;
type BaseInputSizeExternal = 'default' | 'small' | 'medium' | 'large';
type BaseInputSizeCore = 'default' | 'small' | 'large';
type InputElementTypes = 'input' | 'textarea' | 'div';
interface BaseInputProps extends ShareableInputProps {
    /** html-tag инпута */
    inputElement?: InputElementTypes;
    /** Лейбл поля ввода */
    label?: React.ReactNode;
    /** Размер инпута */
    size?: BaseInputSize;
    /** Блок сообщения с подсказкой/ошибкой */
    hint?: React.ReactNode;
    /** id инпута для тестов */
    dataTestId?: string;
    /** ID хинта для тестов */
    dataTestHintId?: string;
    /** Стили  инпута */
    styles: ComponentStyles;
    /** Стили обёртки инпута */
    wrapperStyles: ComponentStyles;
    /** Стили HOC'а withIcons */
    iconsStyles: ComponentStyles;
    /** Ширина label */
    labelWidth?: number;
    /** Флаг, обертка без label, hint и без слижения за минимальной шириной */
    dummyWrapper?: boolean;
    /** Максимальное количество символов инпута */
    maxLength?: number;
    /** Счетчик количества введенных символов */
    counter?: React.ReactNode;
    /** Флаг для отрисовки стилей компонента Select */
    isSelect?: boolean;
    /** Флаг, отображение в траурном режиме */
    isMourning?: boolean;
    /** Дочерние элементы, если используется inputElement === div */
    children?: React.ReactNode;
}
export { BaseInputSize, BaseInputSizeExternal, BaseInputSizeCore, InputElementTypes, BaseInputProps };
