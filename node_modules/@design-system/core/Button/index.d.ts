/// <reference types="react" />
import { ButtonCoreSize, ButtonCoreType, ShareableButtonProps } from "../button-42e030aa";
declare const DEFAULT_PROPS: {
    type: string;
    size: string;
    isLoading: boolean;
};
interface ButtonProps extends Omit<ShareableButtonProps, 'isIcon'> {
    /** Контект кнопки */
    text: string;
    /** Тип кнопки */
    type?: ButtonCoreType;
    /** Размер кнопки */
    size?: ButtonCoreSize;
    /** Пропс для растягивания кнопки на ширину родительского контейнера */
    stretch?: boolean;
}
declare const Button: {
    ({ text, ...props }: ButtonProps): JSX.Element;
    defaultProps: ButtonProps;
};
export { DEFAULT_PROPS, ButtonProps, Button };
