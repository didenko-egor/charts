/// <reference types="react" />
import { ControlledInputTimeProps } from "./ControlledInputTime";
import { TimeValue } from "./utils";
type InputTimeProps = Omit<ControlledInputTimeProps, 'value' | 'onChange'> & {
    /** Значение для компонента */
    value?: TimeValue;
    /** Значение внутри компонента изменилось */
    onChange?: (value?: TimeValue) => void;
};
declare function InputTime({ value, onChange, ...rest }: InputTimeProps): JSX.Element;
export { InputTimeProps, InputTime };
