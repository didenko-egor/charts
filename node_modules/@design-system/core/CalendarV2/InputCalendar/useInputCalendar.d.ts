interface UseInputCalendarProps {
    /** Дата для отображения в календаре */
    value?: Date;
    /** Минимальная дата */
    minDate?: Date;
    /** Максимальная дата */
    maxDate?: Date;
    /** Дата в календаре изменилась */
    onChange?: (value?: Date) => void;
}
/** Логика уведомления об изменении значения в правильном формате */
declare function useInputCalendar({ value, minDate, maxDate, onChange }: UseInputCalendarProps): [string, (value: string) => void];
export { useInputCalendar };
