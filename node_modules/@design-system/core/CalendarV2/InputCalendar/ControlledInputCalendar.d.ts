/// <reference types="react" />
import { CalendarProps } from "../../calendar-34bfb291";
import { InputProps } from "../../Input/index";
import { CalendarPage } from "../../packages/core/shared/PageCalendar/types";
type ControlledInputCalendarProps = Omit<InputProps & CalendarProps, 'value' | 'onSelectedDateChange'> & {
    /** Выбранная дата */
    value?: string;
    /** Страница, на которой открывать календарь */
    defaultPage?: CalendarPage;
    /** Скрыть выходные? */
    hideWeekends?: boolean;
};
declare function ControlledInputCalendar({ size, hintMessage, label, labelWidth, id, name, pseudo, disabled, showHint, labelAside, placeholder, onClick, onKeyPress, uncontrolled, onFocus, onBlur, onChange, maxDate, minDate, value, isError, withoutDefaultMinWidth, defaultValue, defaultPage, locale, hideWeekends, fieldName }: ControlledInputCalendarProps): JSX.Element;
export { ControlledInputCalendarProps, ControlledInputCalendar };
