interface UseInputDateTimeProps {
    /** Дата для отображения в календаре */
    value?: Date;
    /** Минимальная дата */
    minDate?: Date;
    /** Максимальная дата */
    maxDate?: Date;
    /** Дата в календаре изменилась */
    onChange?: (value?: Date) => void;
}
/** Логика уведомления об изменении значения в правильном формате */
declare function useInputDateTime({ value, minDate, maxDate, onChange }: UseInputDateTimeProps): [string, (value: string) => void];
export { useInputDateTime };
