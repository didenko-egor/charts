/// <reference types="react" />
import { FormSize } from "../../types-35606b29";
import { CalendarPage } from "../../packages/core/shared/PageCalendar/types";
import { CalendarProps } from "../../calendar-34bfb291";
import { ControlledInputTimeProps } from "../InputTime/ControlledInputTime";
interface ControlledInputDateTimeProps {
    /** Подсказка */
    hintMessage?: string;
    /** Ярлык */
    label?: string;
    /** Размер */
    size?: FormSize;
    /** Флаг, если компенент не активен */
    disabled?: boolean;
    /** Флаг при ошибке */
    isError?: boolean;
    /** Флаг расположения ярлыка */
    labelAside?: boolean;
    /** Свойства для компонента InputCalendar */
    inputCalendar?: CalendarProps;
    /** Свойства для компонента InputTime */
    inputTime?: ControlledInputTimeProps;
    /** Начальное значение */
    value?: string;
    /** Флаг отображения подсказки / ошибки */
    showHint?: boolean;
    /** Ширина label */
    labelWidth?: number;
    /** Отключает по дефолту заданную минимальную ширину инпута */
    withoutDefaultMinWidth?: boolean;
    /** id поля ввода */
    id?: string;
    /** Страница, на которой открывать календарь */
    defaultPage?: CalendarPage;
    /** Скрыть выходные? */
    hideWeekends?: boolean;
    /** Коллбэк, возвращающий выбранную дату и время */
    onChange?: (value: string) => void;
}
declare function ControlledInputDateTime({ size, disabled, isError, labelAside, label, hintMessage, inputCalendar, inputTime, onChange, value, showHint, withoutDefaultMinWidth, id, labelWidth, defaultPage, hideWeekends }: ControlledInputDateTimeProps): JSX.Element;
export { ControlledInputDateTimeProps, ControlledInputDateTime };
