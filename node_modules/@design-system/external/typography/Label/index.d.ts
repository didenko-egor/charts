/// <reference types="react" />
import React from 'react';
import { LabelFontColor, LabelSize } from "../../types-170abf0a";
interface LabelProps {
    /** Тип лэйбла */
    size?: LabelSize;
    /** Цвет шрифта */
    color?: LabelFontColor;
    /** Атрибут связи с элементом формы */
    htmlFor?: string;
    /** ID для тестирования */
    dataTestId?: string;
    /** Дочерние элементы */
    children: React.ReactNode;
    /** Флаг для наследования цвета текста от родителя */
    inheritColor?: boolean;
    /** Имя тега (при заполненном htmlFor будет игнорироваться) */
    tagName?: 'div' | 'span';
    /** Флаг для отрисовки строчного представления */
    inline?: boolean;
}
declare const Label: React.FC<LabelProps>;
declare const MemoizedLabel: React.NamedExoticComponent<LabelProps>;
export { MemoizedLabel as default, LabelProps, Label };
