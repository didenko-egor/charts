type FormSize = 'small' | 'default';
interface FormProps {
    /** Ширина label у компонентов внутри Form */
    labelWidth?: number;
    /** Флаг расположения label у компонентов внутри Form */
    labelAside?: boolean;
    /** Размер компонентов */
    size?: FormSize;
    /** id для тестов */
    dataTestId?: string;
}
interface FormContextData {
    labelWidthForm?: number;
    labelAsideForm?: boolean;
    sizeForm?: FormSize;
}
type FormContextType = {
    LABEL_WIDTH?: number;
    LABEL_ASIDE?: boolean;
    SIZE?: FormSize;
};
export { FormSize, FormProps, FormContextData, FormContextType };
