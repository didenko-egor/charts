type DropDownWidth = 282 | 376 | 488;
type AlignDropDown = 'left' | 'right' | 'center';
type OnClickItem = (value: string | null) => void;
type OnScrollLock = (newIsLocked: boolean) => void;
type OnChange = (newActiveItems: string[]) => void;
type SelectItem = {
    /** Значение */
    value: string;
    /** Текст, выводимый пользователю */
    label: string;
    /** Неактивный */
    disabled?: boolean;
};
interface SharedSelectDropDownProps {
    /** Элементы списка */
    items: SelectItem[];
    /** Ширина выпадающего списка */
    listWidth?: DropDownWidth;
    /** Массив выбранных value */
    activeValues: string[];
}
export { DropDownWidth, AlignDropDown, OnClickItem, OnScrollLock, OnChange, SelectItem, SharedSelectDropDownProps };
