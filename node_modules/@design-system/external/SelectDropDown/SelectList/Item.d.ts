/// <reference types="react" />
import React from 'react';
import { OnClickItem } from "../../sharedTypes-e165a393";
interface ItemProps {
    /** Значение, передаваемое в обработчики */
    value: string | null;
    /** Текст, выводимый пользователю */
    label: string;
    /** Неактивный */
    disabled?: boolean;
    /** Элемент выбран */
    isActive: boolean;
    /** Обработчик клика по элементу */
    onClick: OnClickItem;
    /** Отображать иконку */
    withIcon: boolean;
    /** Резервировать слева область под чекбокс, если его нет */
    useLeftKeeper?: boolean;
    /** Резервировать область справа (для перекрытия кнопкой закрытия) */
    useRightKeeper?: boolean;
    /** Флаг траурного режима */
    isMourning?: boolean;
}
declare const _default: React.NamedExoticComponent<ItemProps>;
export { _default as default, ItemProps };
