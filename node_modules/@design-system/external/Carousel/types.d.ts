type Command<T = unknown> = {
    execute: (param: T) => void;
    canExecute: boolean;
};
type CarouselColorTheme = {
    color: string;
    background: string;
    disabledColor: string;
    divider: string;
    inverseColor?: string;
    inverseDisabledColor?: string;
};
type BuiltInTheme = 'arctic' | 'science' | 'special-project' | 'recommender';
declare const builtInThemes: {
    [key in BuiltInTheme]: CarouselColorTheme;
};
export { Command, CarouselColorTheme, BuiltInTheme, builtInThemes };
