import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  roles: [1, 2, 3, 4, 5]
};

const user = createSlice({
  name: 'dwdwd',
  initialState,
  reducers: {}
});

export default user.reducer;
